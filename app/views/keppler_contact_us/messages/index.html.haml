= title(t('keppler.titles.index', model: t("keppler.models.pluralize.messages")))

.listing.col.l9.m12.s12
	= entries(@total, @objects)
	%table.bordered
		%tbody
			/ para colocar titul si se quiere
			/ - unless @objects.blank?
			/ 	%tr{style:"background: #fff;"}
			/ 		%th{style: 'padding-left: 10px'}
			/ 			%th.center= t("activerecord.attributes.message.Image")
			/ 			%th.center= t("activerecord.attributes.message.Image")
			/ 			%th.center= t("activerecord.attributes.message.Image")
			/ 			%th.center= t("activerecord.attributes.message.Image")
			/ 			%th.center= t("activerecord.attributes.message.Image")

	
				

			- @objects.each do |message|
				= render 'listing', message: message
			- if @objects.size.zero?
				%tr
					%td{colspan: "4"}
						%center
							= t('keppler.messages.not_found_records', model:  t("keppler.models.pluralize.messages"))

	= paginate @objects, window: 1

.listing-show.col.l3.m12.hide-on-small-and-down.hide-on-med-and-down
	= render 'description'